/**
 * cookie的key集合
 */
export const cookieKey = [
  'session_id', // session_id
  'yt_cluster_id', // cluster_id
  'currentLanguage', // 当前语言
];

/**
 * sessionStorage的key集合
 */
export const sessionStorageKey = [
  'session_id', // 登录态
];

/**
 * localStorage的key集合
 */
export const localStorageKey = [
  'session_id', // 登录态
];
